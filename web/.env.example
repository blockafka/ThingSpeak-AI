# 前端 Vercel 环境变量（二选一，也可都不配，已内置默认后端地址）

# 方式 A：直连后端（CORS 已开启）
# VITE_API_BASE_URL=https://yingx-o5ua.vercel.app

# 方式 B：同域反代（build 时写入 vercel.json rewrites）
# BACKEND_URL=https://yingx-o5ua.vercel.app
